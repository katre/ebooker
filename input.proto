package ebooker;

message Book {
  optional string title = 1;
  optional string author = 2;
  repeated Chapter chapters = 3;
  optional string defaultSelector = 4;
}

message Chapter {
  optional string name = 1;
  optional string url = 2;
  optional string selector = 3;
}
